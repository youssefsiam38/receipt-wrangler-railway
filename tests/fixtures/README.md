# Test fixtures

- `make-receipt.py` generates `receipt.png`, a synthetic 320x420 grayscale receipt image
  ("TEST MART", three line items, total 13.50) drawn from a built-in bitmap font at test time.
  It contains no real personal data and no third-party content; the PNG itself is not committed.
