# Assets

`icon.png` is Receipt Wrangler's 512x512 application icon, copied unchanged from the upstream
repository (`mobile/macos/Runner/Assets.xcassets/AppIcon.appiconset/app_icon_512.png`, part of the
AGPL-3.0 licensed project, © the Receipt Wrangler authors). It is used only as the marketplace card
image so people recognise the product. This template is not affiliated with the Receipt Wrangler
project.
